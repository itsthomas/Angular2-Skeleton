import { Component } from '@angular/core';

// Decorators are instances of a third party component. They start with @ sign
@Component({
  selector: 'my-app',
  template: '<h1>Skeleton Project</h1>'
})

export class AppComponent { }